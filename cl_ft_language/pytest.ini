[pytest]
testpaths = tests
markers =
    gpu: needs a CUDA device (run inside an salloc/srun GPU job)
    slow: takes more than a few seconds
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
